var dist = {};

function defineHead(input) {
  return input;
}

dist.defineHead = defineHead;

interface MetaFlatSchema {
    name: string;
    key: 'charset' | 'name' | 'property' | 'http-equiv';
    description: string;
    suggestions: string[];
    importance?: 'required' | 'important' | 'recommended' | 'optional' | 'deprecated' | 'unknown' | 'not-recommended';
    category?: 'render' | 'device' | 'seo' | 'social-share' | 'unknown' | 'security' | 'privacy' | 'pwa';
    links?: string[];
}
declare const metaFlatSchema: Record<keyof dist.MetaFlat, MetaFlatSchema>;

export { metaFlatSchema };
